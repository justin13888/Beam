node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
.nitro
.tanstack
.wrangler

# Coverage output
coverage/

# GraphQL Codegen (src/gql.ts is tracked as a stub; update via `bun run codegen:full`)

# OpenAPI Codegen — generated at build time from the Rust backend
# Regenerate locally with: bun run codegen:openapi:full
openapi.json
src/api.gen.ts
